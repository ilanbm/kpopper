known:
    api.limit: {v: 10}
judgments:
    d.w: {verdict: known, rests_on: [api.limit], wrong_if: "api.limit > 100", seen: {api.limit: 10}}

    d.z:
      verdict: x
      rests_on: [api.limit]
      wrong_if: "api.limit > 50"
      seen: {api.limit: 10}
