{
  "hooks": {
    "post_write_code": [
      {
        "command": "python3 -c \"import json,sys,os,subprocess; d=json.load(sys.stdin); fp=(d.get('tool_info') or {}).get('file_path') or d.get('file_path') or ''; os.path.basename(fp) in ('GROUNDING.yaml','PROVENANCE.yaml') and subprocess.run(['python3','../../scripts/provenance.py','check'])\"",
        "show_output": true
      }
    ]
  }
}
